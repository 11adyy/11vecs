[project]
name = "vecs"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "11adyy", email = "111adyydev@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "openai>=3.3.1",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "pytest-asyncio>=0.21.0",
]

[project.scripts]
adyyvectordatabase = "adyyvectordatabase:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
